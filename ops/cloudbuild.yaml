# ===========================================
# A2R RAG API - Cloud Build Configuration
# ===========================================
# Builds and deploys to Cloud Run automatically

steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-api:$SHORT_SHA'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-api:latest'
      - '-f'
      - 'ops/Dockerfile'
      - '.'
    
  # Push the image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push'
      - '--all-tags'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-api'
  
  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'rag-api'
      - '--image=us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-api:$SHORT_SHA'
      - '--region=us-central1'
      - '--service-account=rag-api-sa@$PROJECT_ID.iam.gserviceaccount.com'
      - '--vpc-connector=rag-conn'
      - '--vpc-egress=all-traffic'
      - '--min-instances=1'
      - '--max-instances=20'
      - '--cpu=2'
      - '--memory=2Gi'
      - '--timeout=300'
      - '--concurrency=80'
      - '--set-secrets=MONGO_URI=MONGO_URI:latest'
      - '--set-env-vars=MONGO_DB=rag_db,MONGO_COLLECTION=documents,VECTOR_INDEX=rag-chatbot-index'
      - '--set-env-vars=EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2'
      - '--set-env-vars=OLLAMA_BASE_URL=http://ollama-runner.internal:11434,OLLAMA_MODEL=mistral'
      - '--set-env-vars=LANGCHAIN_TRACING_V2=true,LANGCHAIN_PROJECT=A2R-RAG'
      - '--set-env-vars=LOG_LEVEL=INFO,API_VERSION=1.0.0,ENVIRONMENT=production'
      - '--ingress=internal-and-cloud-load-balancing'
      - '--no-allow-unauthenticated'

# Store images in Artifact Registry
images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-api:$SHORT_SHA'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-api:latest'

# Build options
options:
  machineType: 'E2_HIGHCPU_8'
  diskSizeGb: '50'
  logging: CLOUD_LOGGING_ONLY

# Timeout
timeout: '1200s'